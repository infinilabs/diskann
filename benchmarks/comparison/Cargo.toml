[package]
name = "diskann_comparison_benchmarks"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
tempfile = "3.8"
diskann = { path = "../../diskann" } 